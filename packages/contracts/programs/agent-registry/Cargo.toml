[package]
name = "agent-registry"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
solana-program = "=1.18.26"
borsh = "=0.10.3"

# Pin transitive dependencies to avoid edition2024 requirement
blake3 = "=1.5.0"
constant_time_eq = "=0.3.0"
